# -Password-Manager-Tkinter-JSON-
This Python project is a Password Manager built using Tkinter and JSON. It generates strong passwords, stores website credentials securely in a JSON file, and allows searching saved data. The app includes error handling, input validation, clipboard support, and a simple GUI for managing passwords efficiently and safely.
